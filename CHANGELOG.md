# Changelog

所有项目的重要变更都会记录在此文件中。

项目采用 [Semantic Versioning](https://semver.org/lang/zh-CN/) 进行版本管理。

---

## [Unreleased]

### 计划中（需外部基建/专项推进，暂缓）
- Telegram 消息内「已续费」按钮（需公网 HTTPS 回调或长轮询）。
- Web Push 推送（需 VAPID 密钥与订阅管理；当前已有 ntfy/Gotify/Telegram 等推送替代）。
- 日/韩/西/德 界面语言（需完整翻译校对，避免半成品）。

---

## [1.11.2]

### Added
- **面板渲染错误兜底（debug16 后续加固）**：新增 `ErrorBoundary` 组件并包住设置页的通知配置。
  Vue 里子组件渲染抛异常会让整棵子树被卸载，用户看到的就是「某一栏整块消失」且毫无提示
  （debug13 的 Webhook、debug16 的企业微信自建应用都是这个现象）。现在出错只影响那一个面板，
  并把错误原文与反馈方式显示出来，页面其余部分照常可用。
  - 用内存渲染器在客户端渲染路径上实测过：兄弟节点存活、兜底文案与错误原文均正确显示；
    对照组（无边界）则整棵树渲染失败——与两次故障的表现一致。

---

## [1.11.1]

### Fixed
- **企业微信点「自建应用」后通知配置整块消失（debug16）**：根因不在组件逻辑，而在 i18n 文案。
  `notifyCfg.wecomStep4` 里写了裸 `@all`，而 `@` 是 vue-i18n 的**链接消息标记**，运行时编译抛
  `Invalid linked format`，渲染即异常、整个通知组件被卸载（与 debug13 表现同类）。同一条文案里
  作为成员分隔符的 `|` 又是 vue-i18n 的**复数分隔符**，会让后半句被静默截断。三语均改用
  `{'@'}` / `{'|'}` 字面量转义。
  - 这条文案只在「自建应用」模式下渲染（群机器人模式渲染的是另一组步骤），所以群机器人正常、
    一点自建应用就白 —— 与反馈的现象完全对应。

### Added
- **i18n 词条体检（防这类问题复发）**：新增 `frontend/scripts/check-i18n.mjs`，逐条编译全部
  1350 条词条，并挂到 npm 的 `prebuild` 钩子——本地 `npm run build` 与 Docker 镜像构建都会先跑，
  编译失败直接中断构建；同时警告裸竖线、以及 en/ru 相对 zh 的缺键。
  这类错误 `vite build` 阶段完全看不出来、只在浏览器渲染时炸（debug13 与 debug16 两次都是如此），
  所以固化成构建门禁。也可单独执行 `npm run i18n:check`。

---

## [1.11.0]

### Added
- **企业微信自建应用推送（debug15-1）**：企业微信从「只支持群机器人」升级为双模式，
  自建应用模式的配置项与 [CMSHelp 消息配置](https://github.com/guyue2005/CMSHelp/wiki/8.%E6%B6%88%E6%81%AF%E9%85%8D%E7%BD%AE)
  保持一致——**企业ID（CorpID）+ 应用ID（AgentId）+ 应用Secret + API 代理**，
  并可指定**接收成员 / 部门 / 标签**（留空按 `@all` 处理）与**消息类型**（text / markdown / textcard）。
  - `access_token` 按「企业ID+Secret+代理地址」缓存复用（提前 200s 续期），避免触发企业微信频率限制；
    遇 40014 / 42001 / 41001 自动强制刷新并重试一次。
  - 常见错误码给出中文定位提示：81013（不在可见范围）、60020/301014（IP 未加白 / 需代理）、40056（AgentId 错误）；
    `errcode=0` 但 `invaliduser` 非空时也视为失败，不再假装成功。
  - 新增 `POST /api/notifications/wecom/check`，一键校验配置并回显**应用名 / AgentId / 可见范围**统计。
  - 应用 Secret 纳入加密存储（`_SECRET_FIELDS`）。
- **Web 端企业微信配置说明（debug15-1）**：设置 → 通知 → 企业微信内置可折叠图文指引（中 / 英 / 俄）：
  六步取值路径（企业信息取 CorpID、自建应用取 AgentId/Secret、可见范围、@all 与 UserID 的 `|` 分隔写法、
  微信插件扫码后可直接在微信收提醒、先「检查应用」再「测试通知」）、
  **企业可信 IP 白名单警示**，以及 Nginx / Docker socat 两种**反代示例**可展开复制；
  并明确说明 Token 与 EncodingAESKey 仅用于接收消息回调，单向推送无需填写。

### Changed
- 群机器人模式选择 textcard 时自动降级为 text（群机器人不支持文本卡片），避免静默失败。
- 消息按 UTF-8 字节截断到企业微信上限（text 2048B / markdown 4096B / 卡片描述 512B）。

---

## [1.10.0]

### Added
- **Web 端版本检测与更新提示**：应用自动对比当前版本与 GitHub 最新发布版（结果缓存 6 小时），
  有新版时在页面顶部显示提示横幅，并在「设置 → 版本与更新」给出升级命令与更新内容链接；
  升级前提示先备份。新增 `GET /api/system/version-check`。

---

## [1.9.0]

### Added
- **报表导出 CSV**：报表页一键导出各订阅折算月度/年度成本（基准货币）。
- **PWA 可安装**：新增 manifest 与 Service Worker，可添加到手机主屏、离线打开外壳。
- **Home Assistant / API 集成**：新增 `GET /api/integrations/summary`，用 API Token
  轮询即可拿到即将续费与统计，接入 HA 传感器/自动化。

---

## [1.8.0]

### Added
- **忘记密码**：登录页可通过邮箱重置码找回密码（需管理员配置 SMTP）。
- **两步验证 (2FA / TOTP)**：设置页扫码开启，登录需输入验证器 6 位码。
- **API Token**：设置页创建/吊销，供脚本以 `Authorization: Bearer <token>` 访问 API。
- **登录失败限流**：短时间多次失败自动锁定，防暴力破解。

### Security
- **敏感信息加密存储**：各通知渠道的 Token/密钥在数据库中加密保存（密钥由
  JWT_SECRET 派生），读取时解密；历史明文自动兼容，升级不丢配置。

---

## [1.7.0]

### Added
- **通知渠道扩到 14 个**：新增 Server酱 / 企业微信 / 钉钉 / Discord / Slack / ntfy / Gotify。
- **月度预算**：设置里设预算，仪表盘显示进度条与超支预警。
- **免打扰时段**：非紧急提醒在时段内暂缓（到期当天/次日仍发）。
- **每周汇总推送**：按设定的星期几汇总即将续费/已过期项。
- **试用期 / 取消截止日 / 付款卡到期** 字段与到点提醒，避免被自动扣费。
- **CSV 批量导入 / 导出** 订阅。

### Fixed
- 无

---

## [1.6.0]

### Added
- **本地定时自动备份**：每天凌晨自动把整站数据导出到 `data/backups/`（保留最近 14 份），
  管理员可在设置页查看列表并「立即备份」。多一层防丢数据保险。
- **日历订阅 (.ics)**：设置页生成私有订阅链接，把续费日订阅到 Apple / Google / Outlook
  日历，续费日自动进日历并原生提醒（可重置链接）。
- **跟随系统深色模式**：主题新增「Auto」，随系统浅色/深色自动切换。
- **订阅列表搜索**：订阅管理页可按名称 / 套餐 / 备注 / 官网快速筛选。

---

## [1.5.2]

### Fixed
- **修复点击 Webhook 标签导致「通知」配置整块消失**：根因是 Webhook 模板提示文案里的
  `{{ }}` 占位符触发 vue-i18n 消息编译报错。改写文案，占位符示例改由输入框展示。

### Changed
- 「按分类浏览」服务库**继续大幅扩充**：在运营商之外，为 流媒体 / 音乐 / AI / 游戏 /
  VPS / 云存储 / 软件 / 域名 / 教育 / 新闻 / 健身 / 会员 各分类新增约 120 项常用服务。
- Docker Hub 说明与 README 同步为多渠道通知描述。

---

## [1.5.1]

### Changed
- 通知渠道配置从独立页面**收回到「设置」页**内（作为一个区块，含 7 个渠道标签页）。
- 添加订阅「提前提醒」默认改为**倒数 7 天每天提醒**（`7,6,5,4,3,2,1`），仍可自定义。
- 「按分类浏览」服务库**大幅扩充运营商 / eSIM**（英美欧日韩港台印澳加等约 55 家）。

### Fixed
- 修复通知配置 **Webhook 标签页打开空白** 的问题（配置字段缺失时的渲染守卫）。
- 修复 CONTRIBUTING.md 中 `你的用户名` 占位坏链；补充缺失的 CODE_OF_CONDUCT.md。

---

## [1.5.0]

### Added
- **多通知渠道**：在 Telegram 之外新增 飞书 / QQ / Bark / Email / Pushplus / Webhook，
  独立「通知配置」页（7 标签页），每渠道可单独启用与测试（闭合 issue #1 Bark 支持）。
- 备份/恢复（单用户 + 管理员整站）纳入通知渠道配置，重新部署后不丢失。

### Changed
- 仪表盘「分类总览」每个分类内订阅改为按到期时间升序排列。
- Telegram 配置从「设置」页迁移到独立「通知配置」页（设置页改为跳转入口）。

### Fixed
- **数据库断线自愈**：宿主机/容器异常重启后，app 若早于 MySQL 启动不再永久掉入
  安装向导；启动多次重试 + 请求到达时自动重连，前端自愈期间不再误弹向导。

---

## [1.0.0] - 2026-06-19

### Added

#### 核心功能
- ✅ 多用户系统 - JWT 鉴权，管理员和普通用户
- ✅ 订阅管理 - 支持创建、编辑、删除订阅
- ✅ 周期设置 - 支持按日/周/月/年计费，支持一次性买断
- ✅ 自动续费提醒 - APScheduler 定时任务 + Telegram 推送
- ✅ 仪表盘 - 总览支出、即将到期项目、最近订阅

#### 多语言和本地化
- 支持中文 / English / Русский
- vue-i18n 国际化框架
- 用户可选择语言

#### 多主题支持
- 5 个预设主题：浅色、深色、海洋、森林、紫罗兰
- CSS 变量实现主题切换

#### 货币管理
- 支持全球主流货币（USD、CNY、EUR 等）
- 实时汇率更新（open.er-api.com）
- 用户自定义货币功能
- 日期缓存，避免频繁 API 调用

#### 分类管理
- 预设分类：流媒体、AI、游戏、VPS、电信运营商等
- 用户可自定义分类
- 分类排序功能

#### 付款方式
- 预设付款方式：信用卡、Apple Pay、支付宝、微信、PayPal 等
- 用户可自定义付款方式

#### 日历视图
- 苹果风格日历
- 显示所有续费日期
- 交互式日期选择

#### 报表分析
- 支出洞察 - 总支出、平均支出、支出趋势
- 分类排行 - 按分类统计支出
- 永久购买统计
- 即将续费提醒
- 已过期订阅查看
- 按月/年展示

#### 自定义图标
- Emoji 选择
- 图片上传功能
- 本地图标库

#### Telegram 通知
- 创建 Bot Token 验证
- Chat ID 自动获取
- 自定义提醒天数
- 测试消息发送
- 通知日志记录

#### 数据库
- MySQL 8 支持
- 网页安装向导配置连接
- 自动建表和初始化
- 数据库迁移支持

#### 部署
- Docker Compose 一键部署
- Dockerfile 多阶段构建
- Caddy 反向代理 + 自动 HTTPS
- NAS 部署支持（TrueNAS、fnOS、群晖等）
- 环境变量配置

### Changed
- 项目名称：「手机保号通知」→ 「省心订阅 EasySub」
- 数据库架构完全重设计，支持外部 MySQL

### Fixed

### Removed

---

## 版本发布规则

- **主版本号**（Major）：不兼容的 API 更改
- **次版本号**（Minor）：新增功能，向后兼容
- **修订版本号**（Patch）：Bug 修复，向后兼容

---

## 如何贡献更新日志

当你提交 PR 时，请同时更新 `CHANGELOG.md`：

1. 添加你的更改到 `[Unreleased]` 部分
2. 使用对应的标签（Added / Changed / Fixed / Removed）
3. 清晰描述你的更改

---

## 版本历史

- **1.0.0** (2026-06-19) - 初始版本发布
- **开发中** (main 分支) - 开发版本

---

**最后更新**：2026-06-19
